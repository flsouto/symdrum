imgen/   (new repo)

    db/
        1200x720/
        1400x1400/
    tmp/
        1200x720

generate.php 1200x720 100
reindex.php folder
explore.php db/1400x1400

    save_to: [db, symdrumii] -> if target contains 'tmp'
        or
    copy_to: [symdrumii] -> if target not contains 'tmp'
            
    /show/path/to/img.jpg
    -> s (save) -> if target contains 'tmp'
    -> c (copy) -> if target not contains 'tmp'
    -> d (discard)
    -> f (forward)
    -> b (backward)
    
